{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="text-white bg-green-700 hover:bg-green-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 dark:bg-green-600 dark:hover:bg-green-700 focus:outline-none dark:focus:ring-green-800 w-auto">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
